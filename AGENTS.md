# AGENTS.md

## 沟通规范

- 默认使用简体中文交流，除非用户明确要求英文。
- 解释代码、排查问题、总结变更、制定计划时使用中文。
- 遇到英文错误信息、日志、API 名称、命令输出时，可以保留原文，并用中文解释重点。
- 最终回复应简洁说明：改了什么、验证了什么、还有什么风险或后续事项。

## 项目定位

- 本仓库用于开发“考公助手”产品。
- 产品形态包括 React Native App、后台管理端、未来 Web 端，以及 Python 后端服务。
- 后端以 FastAPI 为核心，Agent 能力作为独立模块设计，早期与业务后端放在同一仓库中。
- 架构目标是先保持单仓库高效迭代，同时保留未来拆分 Agent 服务、后台服务、Web 服务的空间。

## 仓库结构约定

- `apps/mobile`：React Native / Expo App。
- `apps/admin`：后台管理端。
- `apps/web`：未来 Web 端。
- `backend`：Python FastAPI 后端。
- `backend/app/modules`：业务模块。
- `backend/app/agent`：Agent、RAG、工具调用、模型适配、工作流。
- `backend/app/workers`：异步任务。
- `packages/api-client`：前端 API Client。
- `packages/shared`：共享类型、常量、协议说明。
- `packages/config`：共享工程配置。
- `docs/plan`：项目计划、阶段计划和进度记录。

## 开发规范

- 修改代码前必须先阅读相关目录和已有实现，遵循项目既有风格。
- 优先做小步、清晰、可验证的改动，避免无关重构。
- 涉及业务边界、数据结构、接口协议、Agent 行为的改动，应同步更新计划或文档。
- 后端业务逻辑应放在 service 层，Agent 不应直接访问数据库，应通过工具或业务 service 获取数据。
- 接口结构、数据模型、错误格式要保持稳定，避免随意改动已有契约。

## 注释规范

- 本项目代码注释统一使用中文。
- 注释应说明业务原因、边界条件、兼容性、复杂逻辑或重要决策。
- 不要为简单赋值、显而易见的语法、普通参数传递添加注释。
- 注释应简洁但不能过于简陋，能帮助后续维护者理解“为什么这样做”。
- 不要写大段空泛说明，不要把代码逐行翻译成自然语言。
- 公共函数、复杂流程、Agent 工具、模型调用、异步任务、数据迁移应有必要注释。

## Agent 开发规范

- Agent 模块应保持可观测、可评估、可追踪。
- Prompt 模板应集中放在 `backend/app/agent/prompts`。
- Agent 工作流应放在 `backend/app/agent/workflows`。
- Agent 可调用工具应放在 `backend/app/agent/tools`。
- 模型供应商适配应放在 `backend/app/agent/providers`。
- RAG 相关逻辑应放在 `backend/app/agent/rag`。
- 每次 Agent 调用应尽量记录会话、消息、工具调用、模型、耗时、token 用量和失败原因。
- Agent 输出涉及学习建议、题目讲解、申论批改时，应避免编造题目来源、考试政策或用户学习记录。

## 阶段推进规范

- 开发按阶段推进，每个阶段应有清晰目标、范围、完成标准和当前进度。
- 阶段开始前，应更新 `docs/plan/phase-plan.md` 中的当前阶段详细计划。
- 阶段进行中，应持续更新当前进度，不要等阶段结束后一次性补写。
- 阶段完成后，应总结已完成内容、验证结果、遗留问题和下一阶段建议。

## Git 提交规范

- 需要按阶段提交代码，每完成一个有明确边界的阶段或子任务，应进行一次提交。
- Commit message 使用中文 Conventional Commits。
- 常用格式：
  - `feat: 增加题库基础模块`
  - `fix: 修复答题记录保存失败`
  - `refactor: 调整 Agent 工具调用边界`
  - `docs: 更新阶段计划`
  - `test: 补充刷题流程测试`
  - `chore: 初始化仓库结构`
- 提交前必须总结改动、风险和验证结果。
- 不要主动执行 `git push`，除非用户明确要求。
- 不要重写历史、reset、checkout 或删除他人改动，除非用户明确要求。

## 验证规范

- 声称完成前必须进行与改动范围匹配的验证。
- 后端改动优先运行相关测试、类型检查、格式化或启动检查。
- 前端改动优先运行类型检查、lint、构建或关键页面启动验证。
- 文档或目录类改动至少检查文件路径、目录结构和内容是否符合要求。
